### 2017302580107-肖敏-homework6

+ p26
```
a. 由于TCP的序号字段为4个字节，则其最大序号为2^32,则根据TCP是字节流编号的，L的最大值为2^32字节  
b. 根据L=>最大值求出报文数N N=2^32/536=8012999  
       =>求出报文的总字节数，总头部长加上数据长度：N*66 + 2^32 (byte)=4.824∗10^9 byte
       =>传输的时长t=总字节数/155Mbps=4.824∗10^9 byte/155Mbps=4.824*8 * 10^9 / 155 /10^3=249s
```


+ p43
```
1.无需进行流量控制——如果接受缓存足够大
2.无需进行拥塞控制——没有丢包或者超时
3.无需控制——发送缓存满载时自动慢下来
```

+ p46
```
a. 最大窗口长度（以报文估计）：
W_max=10Mbps * RTT / MSS= 10Mbps*150ms/1500b=125
b. 平均窗口长度（以报文估计）：
W_avg=(1+0.5)/2*W_max=93.75
平均吞吐量（以bps计算）：
T_avg=W_avg*MSS*8/RTT=7.52Mbps
c. 回复时间：
W_now=W_max/2=62.5
T_recover=(W_max - W_now) * RTT=(125-62.5)*150=9.375s
```
